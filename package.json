{
  "name": "NextEpisodeServer",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "debug": "meteor run --inspect",
    "test": "meteor test --once --driver-package meteortesting:mocha",
    "deploy": "sh deploy.sh"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.49",
    "expo-server-sdk": "^2.3.3",
    "fcm-push": "^1.1.3",
    "jsonwebtoken": "^8.2.1",
    "meteor-node-stubs": "^0.3.2"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
